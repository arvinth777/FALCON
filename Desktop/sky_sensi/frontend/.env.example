# Environment Configuration for Sky Sensi Frontend

# Backend API Configuration
# For development, use the local backend server
VITE_API_BASE_URL=http://localhost:3001/api

# For production, use your deployed backend URL
# VITE_API_BASE_URL=https://your-backend-domain.com/api

# Development Settings
# Enable additional logging and debugging features
VITE_ENV=development

# Timeout (ms) for frontend API requests; increase cautiously for slow networks
VITE_API_TIMEOUT=30000

# Enable console debug logs for troubleshooting (set to true locally only)
VITE_ENABLE_DEBUG_LOGS=false

# Production Settings (uncomment for production builds)
# VITE_ENV=production
# VITE_API_BASE_URL=https://your-production-backend.com/api

# Optional: Google Analytics or other tracking IDs
# VITE_GA_ID=G-XXXXXXXXXX

# Optional: Sentry DSN for error tracking in production
# VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Optional: Enable service worker for offline functionality
# VITE_ENABLE_PWA=true

# Note: All environment variables must be prefixed with VITE_ to be accessible in the browser